spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  kafka:
    consumer:
      auto-offset-reset: earliest
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

logging:
  level:
    io.repositree: DEBUG
